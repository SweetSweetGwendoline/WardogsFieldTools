"use strict";

// Every runtime position uses this grid. Distances and radii use metres.
const CoordinateSystem = Object.freeze({
    id: "calculator-grid-v1",
    metersPerUnit: 100,
    xDirection: "east",
    yDirection: "north",
});
